<ul class="navbar-nav">
  <li class="nav-item">
    <a class="nav-link active" aria-current="page" href="/">Home</a>
  </li>
  <% if user_signed_in? %>
  <!-- if user_signed_in?-->
  <li class="nav-item"><!-- user_path(@user.id), CRESPO-->
    <a><%= link_to "Profile2", user_path(current_user.username), class: "nav-link active" %></a>
  </li>
  <li class="nav-item">
    <a class="nav-link active" aria-current="page" href="<%= new_tweet_path %>">
      Tweet
    </a>
  </li>
  <li class="nav-item">
    <a class="nav-link active" aria-current="page">
      @<%= current_user.username %>
    </a>
  </li>
  <li class="nav-item">
    <div class="d-grid gap-2 d-md-block">
      <button class="btn btn-primary" type="button">
        <%= link_to "SIGN OUT", destroy_user_session_path, method: :delete %>
      </button>
    </div>
  </li>
  <% end %>
</ul>
